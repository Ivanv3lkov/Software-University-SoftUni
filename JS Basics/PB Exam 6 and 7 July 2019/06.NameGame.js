function solve(input) {
    let name = input.shift();

    while (name != "Stop") {

        for (let i = 0; i < name.length; i++) {
            let num = Number(input.shift());
            let digit = name.charCodeAt(i);



        }

        name = input.shift();
    }
}
solve([
    'Ivan', '73', '20',
    '98', '110', 'Ivo',
    '80', '65', '87',
    'Stop']);